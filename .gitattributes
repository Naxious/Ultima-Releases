# Cryptographic hashes and signatures cover exact bytes; never normalize release objects.
channels/**/release.json -text
channels/**/release.json.sig -text
channels/**/launcher.json -text
channels/**/launcher.json.sig -text
releases/** -text
